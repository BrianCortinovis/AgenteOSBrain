{
  "name": "agenteos-brain-desktop",
  "version": "0.2.0",
  "description": "Agent OS Brain — Desktop App (Tauri)",
  "private": true,
  "scripts": {
    "build": "node scripts/build-all.js",
    "prebuild:app": "node scripts/build-all.js",
    "build:app": "cd src-tauri && cargo tauri build --target aarch64-apple-darwin",
    "build:universal": "node scripts/build-all.js && cd src-tauri && cargo tauri build --target universal-apple-darwin",
    "dev": "cd src-tauri && cargo tauri dev",
    "icons": "cd src-tauri && cargo tauri icon icons/app-icon.png"
  }
}
